@{ 
    string newPassword = ViewBag.newPassword;
}

<div class="container mt-3 pb-5 bg-white">
    <div>
        <h3>Your temporary password is @newPassword. Please <a asp-controller="Account" asp-action="Login">log in</a> to change your password</h3>
    </div>
</div>